<div class="container col-md-4 col-md-offset-4 pan">
  <%= form_for @ut_student  , :html => {class: ''}  do |f| %>
  <% if @ut_student.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@ut_student.errors.count, "error") %> prohibited this ut_student from being saved:</h2>

    <ul>
      <% @ut_student.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <div class="input-group ">
    <%= f.text_field :email ,class:  'form-control' ,placeholder: "Email"%>
    <span class="input-group-btn">
     <%= f.submit "send",class: 'btn btn-primary ' ,type:'submit'%>
   </span>
 </div>
 <br>

 <% end %>

 <%= link_to 'Back', ut_students_path ,class: 'btn btn-danger'%>
</div>